apiVersion: v1
kind: Service
metadata:
  name: {{ include "ax-agent.name" . }}
  labels: {{- include "ax-agent.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector: { app: {{ include "ax-agent.name" . }} }
  ports: [{ name: http, port: 8000, targetPort: http }]
