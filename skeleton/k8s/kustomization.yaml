# kubectl apply -k k8s/   — 파일 순서대로 적용된다
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ax-pilot
resources:
  - 00-namespace.yaml
  - 30-configmap.yaml
  - 11-deployment.yaml
  - 20-service.yaml
  - 40-ingress.yaml
  - 50-netpol.yaml
# Secret은 여기 넣지 않는다:
#   kubectl -n ax-pilot create secret generic agent-secret --from-literal=LLM_API_KEY=... --from-literal=DB_DSN=...
